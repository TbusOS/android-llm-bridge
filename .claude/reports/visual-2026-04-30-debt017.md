---
title: DEBT-017 visual + L-017 端到端验证
date: 2026-04-30
status: pass
---

# DEBT-017 · backend health probe 视觉 + L-017 验证

## 验证范围

DEBT-017 把 `LlmBackendCards` 从"永远 —"改成 6 状态实时渲染（reachable=true / down /
unprobed / planned / error / loading）+ 后端新增 `GET /playground/backends/{name}/health`
端点。属于"接通新 path"档，必须真浏览器 hit 一次（L-017 铁律）。

## 三道闸（mockup v2）

| 闸 | 命令 | 结果 |
|---|---|---|
| 1 verify.py | `python3 sky-skills/.../verify.py docs/webui-preview-v2.html` | OK — 1 file passed |
| 2 visual-audit.mjs | `node sky-skills/.../visual-audit.mjs docs/webui-preview-v2.html` | exit 0（pre-existing dev-strip hollow warns 与本次无关） |
| 3 screenshot.mjs | `node sky-skills/.../screenshot.mjs ...` | saved `mockup-v2-after-debt017.png` |

mockup 共追加 5 张状态卡演示（unprobed / planned / down / fetch-error / probing）+
1 个 section be-card--empty，配合新 BEM 修饰符 `.be-stat--full` /
`.be-stat-value--ellipsis` / `.be-card--empty`。

## L-017 真浏览器（Playwright）

- 起 `uv run alb-api --host 127.0.0.1 --port 8765`（dev / 默认 localhost:11434 探测）
- 浏览 `http://127.0.0.1:8765/app/dashboard`
- 等待 `.be-card` selector + 2.5 s 让 health refetch 至少跑一轮

### 结果

```json
{
  "url": "http://127.0.0.1:8765/app/dashboard",
  "cardCount": 4,
  "consoleErrorCount": 0,
  "consoleErrors": [],
  "runtimeText": [
    "down", "down", "All connection attempts fai…",
    "planned · not implemented",
    "planned · not implemented",
    "planned · not implemented"
  ],
  "screenshot": ".claude/reports/screenshots/2026-04-30-debt-017/dashboard-debt017-prod.png"
}
PASS
```

- 4 cards 渲染（ollama + 3 planned：openai-compat / llama-cpp / anthropic）
- 0 console errors
- ollama 卡显示 down / down / 截断错误信息 → 验证 down 状态布局 + ellipsis 修饰符
- 3 个 planned 卡显示 `planned · not implemented` → 验证 planned 状态 + `.be-stat--full` 跨列布局
- 截图归档：`.claude/reports/screenshots/2026-04-30-debt-017/dashboard-debt017-prod.png`

### 6 状态覆盖说明

L-017 真浏览器拍到 **down × 1 + planned × 3** 共 4 卡 / 2 状态。

剩余 4 状态（up / unprobed / error / loading）由 pytest 覆盖（5 个新 backend
tests + 既有 ABC 默认路径），渲染走相同的 Record-based dispatch 表 + BEM class 体系，
不存在 inline 分支差异。

未真渲染的状态 + 验证手段：

| state | pytest fixture | 触发条件 |
|---|---|---|
| up | `test_health_with_method` (_BackendWithHealth) | 真有 ollama daemon + ALB_OLLAMA_URL 指向 |
| unprobed | `test_health_no_probe_wired` (_FakeBackend 继承 ABC) | 实现了 backend 但 health() 未 override |
| error | useQueries `isError` | 网络断 / endpoint 5xx |
| loading | useQueries `isLoading` | 首次 paint，未 settle |

## 三道闸 + L-017 都过。可以 ship。
