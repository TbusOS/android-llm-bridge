# F.7 端到端验证报告 · 2026-04-29

## 验证目标

F.7 让 KpiStrip 4/4 真实数据 + 修 DEBT-011 + label 区分 LiveCard
"现"（瞬时）vs KPI "5 分均"（窗口）。关闭 DEBT-003 + DEBT-004 +
DEBT-011。

## 验证手段

按 L-017 规则：起新版 alb-api（含 F.7 改动）→ 真实 ollama gemma4:e4b
跑 chat → 拉 /tools / /metrics/summary / /devices / /sessions →
Node 模拟 buildKpis() 与 useAuditStream 分桶逻辑。

## KPI strip 结果

```
Devices          | 1 / 1       | AdbTransport
Sessions         | 4           | all idle
MCP tools        | 33          | 11 categories         ← DEBT-003 关
LLM throughput   | 11.4 tok/s  | 5m avg · 12 samples   ← DEBT-004 关
```

DEBT-003 关闭：MCP tools 不再写死 "21"，显示真实 33。
DEBT-004 关闭：LLM throughput 不再 "—"，显示 5m 窗口均值 11.4 tok/s。

## label 区分语义验证

- LiveSessionCard.tps "tok/s · 现" / "tok/s now" — 最近 1 个 sample
  瞬时值
- KPI throughput "5m avg · N samples" / "5 分均 · N 个采样" — 5min
  窗口聚合均值

两个数字 legitimately 不同（瞬时 vs 平均），label 让用户读得懂，避免
"为啥两边对不上"困惑（来自 F.6 arch reviewer #4 强制要求）。

## DEBT-011 dual-bucket cap 验证

合成 550 事件（50 business + 500 metric，模拟 500s 长 session）：

- 旧单 cap 200：business 仅存活 18/50（**丢 32 条**）
- F.7 dual cap（business 200 + metric 60）：business 50/50 保完整

修复有效。代价：metric cap 60 = SPARK_WINDOW，与 useLiveSession 对齐，
不损失 spark 视觉。

## 后端契约验证

- GET /tools 返回 33 工具 / 11 类 ✓
- GET /metrics/summary?window_seconds=300 返回完整 mean/p50/p95/max/min
  + sample_count + total_tokens ✓
- 645 pytest pass · 敏感词 0 · offline-purity 5 files clean

## 关闭决议

- ✅ DEBT-003 closed
- ✅ DEBT-004 closed
- ✅ DEBT-011 closed

## 后续

- F.8 阶段补 Playwright 视觉端到端 + 全模块 /preflight
